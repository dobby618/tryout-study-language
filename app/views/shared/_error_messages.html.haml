- if object.errors.any?
  #error_explanation
    %h2= I18n.t("errors.messages.not_saved", count: object.errors.count)
    %ul.alert.alert-danger
      - object.errors.full_messages.each do |message|
        %li= message
